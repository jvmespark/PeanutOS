#include "user.h"

void echo(char* str) {
    //kprint(str);
}

void parseUser(char *command, char *str) {
    //kprint(command);
    //if (strcmp(command, "echo") == 0) {
        //echo(str);
    //}
}